{% macro prototypeValues(valsChildren) %}
    {% import '@contact-core/macros/form_type.html.twig' as fields %}

    {{ fields.field(valsChildren.name, "", {
        'class': 'col-12 my-1 mx-0 px-0', 'il_class': {'label': 'col-4', 'input': 'col-8'},
        'dataset': {'ord': 0}
    }) }}
    {{ fields.field(valsChildren.description, "", {
        'class': 'col-12 my-1 mx-0 px-0', 'il_class': {'label': 'col-4', 'input': 'col-8'},
        'dataset': {'ord': 0}
    }) }}
    {{ fields.field(valsChildren.backgroundColor, "", {
        'class': 'col-3 mx-0 px-0', 'value': "#FFFFFF",
        'dataset': {'ord': 0}
    }) }}
    {{ fields.field(valsChildren.borderColor, "", {
        'class': 'col-3 mx-0 px-0', 'value': "#FFFFFF",
        'dataset': {'ord': 0}
    }) }}
    {{ fields.field(valsChildren.textColor, "", {
        'class': 'col-3 mx-0 px-0', 'value': "#000000",
        'dataset': {'ord': 0}
    }) }}
{% endmacro %}
